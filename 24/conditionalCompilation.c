  /* 
        if(setMonth(pDate, 56) == E_INVALID_MONTH)
        {
            #ifdef DEBUG
                fprintf(stderr, "%s:%d:testDate():Invalid month checking successful\n", __FILE__, __LINE__); 
            #else 
                fprintf(stderr, "testDate():Invalid month checking successful\n", __FILE__, __LINE__);
            #endif 
        }
        else 
        {
            #ifdef DEBUG
                fprintf(stderr, "%s:%d:testDate():Somewithing went wrong. existing the app\n", __FILE__, __LINE__); 
            #else 
                fprintf(stderr, "testDate():Somewithing went wrong. existing the app\n"); 
            #endif 
            exit(EXIT_FAILURE)
        }
    */